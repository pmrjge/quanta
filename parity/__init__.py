"""Parity harnesses: plain-mlx.core references diffed against the mlx.nn runtime.

Dev-only tooling (not packaged). The reference forwards here are deliberately
independent transcriptions of the source ``modeling_deepseek.py`` math so a bug in
the runtime shows up as a numeric divergence rather than being mirrored.
"""
