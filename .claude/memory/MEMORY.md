# quanta — project memory index

## feedback
- [MLX-only implementation](feedback_mlx_only.md) — everything in MLX, incl. the parity reference (plain mlx.core); no torch port
- [Streaming / layer-wise loading](feedback_streaming_layerwise.md) — stream sliced tensors; one layer resident at a time; never load full model/tensors
- [Kimi ≠ DeepSeek-V3](feedback_kimi_not_deepseek.md) — verify each model's formats empirically; don't assume DeepSeek parity (offset-binary int4 was the bug)
- [Self-contained artifacts](feedback_selfcontained_artifact.md) — baked bundle holds config.json+manifest+safetensors+metadata, relative refs only
- [Memory safety / no unbounded allocs](feedback_memory_safety.md) — guard O(T²) allocs to fail loud before allocating; a benchmark OOM'd + rebooted the host
- [Batched RoPE bf16 drift](feedback_batched_rope_bf16.md) — hand-rolled batched mx.fast reimpls drift at bf16 on REAL values + compound across layers; loop the per-stream kernel; gate B=1 bit-exact + B>=2 greedy-exact

## project
- [Model targets (multi-model)](project_model_targets.md) — SERVED FLEET now 5+ (InternLM2.5 9 / Qwen3.6-35B 19 / Nemotron-Super 68 / DSV4 180 / Nemotron-Ultra 306 / Nex-N2-Pro 214 GiB), one resident at a time; grew from the 2026-05-28 three-keeper set; #152/#153/#158-160 paged+tree-spec CLOSED; DSV4 ~180GiB int4/gather_qmm
- [Nex-N2-Pro = Qwen3.5-397B-A17B — ACTIVE (N3 serving)](project_nex_n2_pro.md) — post-trained qwen3_5_moe, the EXACT quanta.qwen35 arch ⇒ validate-at-scale+bake (Super→Ultra), NOT build-from-scratch; int4-g64 SHIPPED 214 GiB (~lossless +0.68% ppl); N0–N3-2 done (eos/1M-config, layer parity @397B, ppl arbiter, batched re-gate, qwen3_coder/qwen3 parsers byte-identical to Nemotron's); remaining N3 = 1M needle, paged-KV, MInference on the 15 full-attn layers, fused-GDN step, multi-stream >B=32; handover PLAN_nex_n2_pro.md
- [Nemotron-Ultra-550B — int4-RTN SHIPPED (306 GiB)](project_nemotron_ultra.md) — nemotron_h hybrid; U1 caught a REAL bug (group-wise gated RMSNorm, 42% off, self-consistent so old test missed it — fixed Super too); U3 retired AWQ→RTN e2e (+24.3% vs +0.3%, recon≠e2e); native-MTP M0–M4 (B=1 spec <1×, best 0.92×); fused-SSD-step graduated +36%@B32 (49.4→67 tok/s); paged + tree-spec-over-paged done; handover PLAN_nemotron_ultra.md
- [Batched serving operating point](project_batched_operating_point.md) — prod runs batched decode at B=32; form-2 persistent BatchedMambaState is the prod path; Mamba batching is the lever, not attn
- [#18 batched KV arena — DONE (M0–M5)](project_kv_arena_18.md) — killed DSV4 per-stream KV-update loop via max_batch arena (1 scatter + 1 gather); kv_arena DEFAULT ON since M4; M0✅41a4d0f M1✅6f33cc1 M2✅05d1171 M3✅bf7af6b M4✅e08888d (Approach-B prefill→seed_comp migration, dispatch by cache TYPE, free_cache lifecycle) M5✅f4935b5 (real-model 3-path B-sweep on DSV4-Flash int4-g64: arena greedy-exact vs per-stream loop, **+37% decode tok/s @ B=32**, arena/bat monotone in B); **#18 COMPLETE**; durable handover repo PLAN.md; cadence: single thread, commit+STOP per milestone
- [#153 batched-paged KV — DONE all keepers (DSV4 M0–M4 + Nemotron/InternLM2.5/Qwen3.6)](project_paged_batched_153.md) — bring #18 loop-kill to the PROD paged path; M0✅62609ba generic batched scatter/gather ON PagedKVCacheManager (write_*_batched/gather_*_batched), serves single-stream latent AND k/v keepers in one primitive; M1✅c442c31 dense stepper on the paged arena via _PagedKVArena adapter (gated self._paged_kv_batched←PAGED_KV_BATCHED_DEFAULT=False, M3 flips; bit-exact gate); M2✅35dcd78 COMPRESSED stepper — batches ONLY the latent (rides _PagedKVArena), derived ckv/ikv/ring stay PER-STREAM ⇒ paged snapshot lifecycle UNCHANGED (PLAN's "batch the derived/_CompArena" guess SIDESTEPPED — that's not the lever; smallest-safe-change), new paged-hybrid path in decode_step_compressed_batched (arena+rows+lcs, comp=None), 3-way dispatch, BIT-exact gate _run_compressed_stepper (max|Δ|=0, ratio-4+idx & ratio-3, B=4+B=1); gate `uv run --with numpy python -m parity.dsv4_paged_batched_test`; DSV4 M3✅d19a254 (graduated PAGED_KV_BATCHED_DEFAULT ON; NO _step_paged/omlx change — M1/M2 flag-dispatch already wired _step_paged→step_batch→_decode_batched_single paged_path; gate dsv4_paged_latent_test §C `_run_engine_batched` drives the REAL _DSV4BatchedSession decode over ragged B=3 crossing block boundaries: ON==OFF bit-exact |Δ|=0 + greedy-exact vs discrete argmax_mismatch=0 + decode_snapshots=5 — §A/§B were single-vs-single so 0; §C is FIRST batched-vs-single ⇒ greedy-exact not bit-exact, gated argmax-stable not a tight logit bar); DSV4 M4✅cb2476b (deferred solo-GPU bench `parity/dsv4_paged_batched_bench.py` drives REAL _DSV4BatchedSession paged decode, distinct prompts, flips _paged_kv_batched: loop==loopkill **BIT-exact EVERY B** — both run the one batched SDPA, latent store |Δ|=0 per M0 — AND **+13% tok/s @B32&B48** on DSV4-Flash int4g64 ~180GiB/peak192; SMALLER than #18 M5's +37% bc DSV4 batches ONLY latent (derived per-stream, M2) + MoE dominates decode; flag already ON since M3 model-free so M4 confirms+quantifies, comment-only paged/__init__.py update); **DSV4 #153 core M0–M4 COMPLETE — whole #153 DONE across all keepers**; PLAN_153.md status bumped to DONE in-repo at 861ccc1 (doc-only). MULTI-MODEL follow-on (user: nemotron→internlm2→qwen3.6, IN ORDER, one commit per milestone): shared `modeling.batched_attention.batched_decode_attention_padded` (k/v sibling of _PagedKVArena); Nemotron✅833c8a4 (wire)+7f49bd9 (real-model bench `parity/nemotron_paged_batched_bench.py`: greedy-exact loop==loopkill B∈{1,32,48}, **+18% tok/s @B48 / +15% @B32**, per-stream loop REGRESSES past B32 while loopkill holds ⇒ win GROWS with B; NOT marginal) → default GRADUATED ON via **Nemotron-scoped** NEMOTRON_PAGED_KV_BATCHED_DEFAULT=True (shared PAGED_KV_BATCHED_DEFAULT stays OFF so DSV4 untouched + DSV4 M3 not preempted; Nemotron BEST_BATCH already 32, reaffirmed e64ffbf); InternLM2.5✅fad71bb (wire)+c1db9f6 (bench+GRADUATED ON) — killed per-stream KV loop in BOTH decode_batched (bf16 model.py + packed runtime.py) via NEW single-sourced shared helper `batched_decode_attention_kv` (modeling.batched_attention: per-stream .update() loop OR paged write_batched+gather_batched+padded SDPA; Nemotron's _fused_attn_layer still inlines the equiv), gate internlm2_batched_attention_test §C bit-exact + default-ON pin; real-model bench `parity/internlm2_paged_batched_bench.py` (int8g64 7B, distinct prompts, raw token-ids — no tokenizer): greedy-exact loop==loopkill B∈{1,32,48} AND **3.20× @B32 (104→332 tok/s) / 3.16× @B48** (per-stream loop REGRESSES past B32 while loopkill flat ⇒ win doesn't fade), FAR bigger than Nemotron bc DENSE/ALL 32 layers attention ⇒ graduated ON via **InternLM2.5-scoped** INTERNLM2_PAGED_KV_BATCHED_DEFAULT=True (shared PAGED_KV_BATCHED_DEFAULT now governs DSV4 ONLY, stays OFF so DSV4 M3 not preempted); **Qwen3.6 (qwen35) option B ✅ DONE — M0–M4** (UNPAGED+hybrid 30GDN+10GQA): dequant-bf16 mixer projections reordered across batch-M (loopkill diverged at B≥2, |Δlogit|≈1.3), so built a PACKED+CHUNKED runtime — `_load_block(packed)` holds mixer projections as `nn.QuantizedLinear` + loop-kill steppers apply them in ≤8-row chunks (`QWEN35_LOOPKILL_CHUNK`; M0 c503657 proved chunked-≤8 `quantized_matmul`==per-stream M=1 bit-for-bit, full-batch reorders @B≥12). M0✅c503657 M1✅cf299c3 (GDN `_gdn_step_batched` chunk) M2✅9350482 (GQA `_project_chunked`+`o_proj` chunk; ONE fused SDPA across B kept) M3✅0bacba8 (wire `packed`, `loopkill⇒packed` enforced `_check_loopkill_requires_packed`, `qwen35_forward_test` packed==bf16 greedy-exact |Δ|=1.3e-6, **graduated packed=True**) M4✅6231a1d (real-model `qwen35_batched_bench.py` Qwen3.6-35B-A3B int4g64 loaded PACKED: greedy-exact loop==loopkill EVERY B AND **1.63×@B32** / 1.20·1.45·1.67×@B4·8·16, win grows w/ B; **graduated QWEN35_BATCHED_LOOPKILL_DEFAULT=True**, `from_inner` got a `loopkill` override; B=4 latency-first orchestrator pin LEFT untouched — B=32 is the bench point not the serving pin). MoE (gather_mm) + `gdn_step` (fp32) already batch-invariant — untouched. item-1✅14e01b9 (B=48 within-noise gate). handover PLAN_153.md
- [Qwen3.6 routed-expert gather_qmm packing — DONE M1✅a6b3b49 M2✅d17882e M3✅f720fda](project_qwen35_experts.md) — keep int4 experts PACKED + mx.gather_qmm (not dequant-bf16 + gather_mm); **63→20 GiB resident**; batch-invariant (no #153 batch-M re-open) + greedy-exact; flag `packed_experts` (**default ON since M3**, rule 4, independent of option-B mixer `packed`); M1=moe.py `_routed_sparse_packed` + `qwen35_moe` dict auto-detect + `set_experts_packed` + `_moe_packed_parity` gate (d_pack=1.8e-7, NO act-cast: gather_qmm handles act≠scale dtype); M2=wired `packed_experts` thru artifact.py `moe_packed`/`_packed_triplet` + `_load_block` + `_block_arrays` (dict-aware: evals packed/scale/bias not int meta) + both runtime `__init__`s + `from_inner` (detects from layers); shim UNCHANGED (rides constructor default → M3 flip auto-upgraded serving); gate `_packed_experts_forward_parity` assembled-model greedy-exact |Δ|=1.07e-6; **M3✅f720fda** SOLO-GPU real-model gate `parity.qwen35_batched_bench experts` (loads bake TWICE, 1 model resident at a time): **resident 63.4→20.3 GiB (0.32×, −43 GiB)** + greedy-exact 48-tok + ppl unchanged 2.3150→2.3204 (rel 0.24%, top-1 agree 0.9858) on REAL prose (repo PROSE fixture + Qwen35Tokenizer; NOT synthetic ids — near-uniform inflates the NLL swing); bf16-dequant is the LOSSIER path (rounds weight to bf16 pre-matmul; gather_qmm keeps full precision) → **graduated packed_experts=True default** in both `__init__`s; template dsv4/moe.py; handover PLAN_qwen35_experts.md
- [InternLM2.5 speed levers — EAGLE spec-decode DONE (M0–M3, 1.42× lossless @k=2) + MInference prefill](project_internlm2_eagle.md) — InternLM2.5 = under-optimized keeper (dense ⇒ no MoE levers; only keeper w/ NO spec + full O(T²) prefill). EAGLE model-agnostic (spec_core + EagleDrafter). M0✅9582072 M1✅357a507 M2✅09306da M3✅ec0f6f3. **M3 KEY (generalizable): a bf16 EAGLE drafter is a NET SLOWDOWN on a fast quantized target** (k=2 0.91×) — `mean_accept` saturates ~1.81 AND the draft step is dominated by the frozen 92k-vocab head proj run k×/round in bf16; **lever = QUANTIZE the drafter (body+head), NOT more training** (plateaued): k=2 bf16 0.91×→int8 1.34×→**int4 1.42×**, accept held, spec==greedy. k=2 optimal; 18ms target verify is the decode floor (free-drafter ceiling ~1.77×) ⇒ shrink+retrain FORGONE. Plumbing lossless-safe: `spec_core` opt-in `head_bits` (draft-only; default None byte-unchanged for ALL callers) + bench `IL2_QUANT_BITS` PTQ. Staged-anneal driver `eagle_refine_internlm2.py` (SGDR warm-restarts) lifted holdout 0.409→0.448→0.460. 2nd track = MInference sparse prefill (lossy, **M0 ✅ STARTED** — see [[project-internlm2-minference]]). Spec-on-MoE NOT diluted (batched-tree #157, DSV4 3.77×; MoESD)
- [InternLM2.5 sparse prefill — MInference family, COMPLETE M0–M10](project_internlm2_minference.md) — 2nd InternLM2.5 lever after EAGLE; lossy ⇒ **ppl-gated, NOT parity**. per-head (kind+params) selectors + grouped-gather fold (default-on) → up to 4.3×@64K/layer; +0.04% ppl @ 7-block doc but +31.8% @ 16K code corpus; substrate now transfers to Nex-N2-Pro's 15 full-attn layers. KEY DECISION (user: "reuse substrate first"): do NOT build from scratch — the bounded-memory chunked block-gather *execution* (`gather_sparse_attention`/`sparse_prefill_mask`, `threshold=1.0`==dense) already exists validated in `quanta.modeling.xattention`; MInference vs XAttention differ only in *selection*. M0=wired `self.sparse: XAttnConfig|None` hook into `InternLM2Attention` (default None=dense **byte-unchanged**) routing from-scratch prefill over GQA-repeated kr/vr + model-free gate `internlm2_xattn_test` (keep-all mask==dense **0.00**, gather 5e-8, causal-safe, min_seq-gated). **M1 next**=real-model long-doc ppl sweep on int8g64 bake (solo GPU); `ppl_long.py` is **Kimi-bound** (`layer.self_attn`) ⇒ write `internlm2_ppl_sparse.py` sibling (InternLM2 uses `layer.attention.sparse`). M2+=vertical-slash/A-shape selectors on the same gather execution
- [Forward bug RESOLVED](project_forward_bug_resolved.md) — catastrophic ppl was source int4 offset-binary dequant, not experts; bf16 ppl 3.31; int3-floor measurable
- [Bake strategy](project_bake_strategy.md) — GPTQ experts (activation-weighted DP, Woodbury), int8 non-experts, bf16 shared/norms; shipped int2/int4-g64; e2e ppl gate
- [Tokenizer + two-eos](project_tokenizer_eos.md) — tiktoken 163584+256; [EOS]163585 vs gen-eos <|im_end|>163586 vs [EOT]163593; dual-mode encode + jinja template
- [oMLX serving contract](project_omlx_serving_contract.md) — raw-output engine + import-hook autopatch; reasoning/tools parsed oMLX-side; Kimi tool-patch, Nemotron none
- [EAGLE-3 spec-decode](project_eagle3_speculative.md) — BUILT but FORGONE on Kimi (benchmarked no-op; MoE dilutes win); drafter/train findings kept for a future dense target
- [Flash tiled SDPA for long ctx](project_flash_sdpa_longctx.md) — prior project ran 256K via flash SDPA; runtime attention must use mx.fast.sdpa (naive only for parity)
- [Prefill optimization landscape](project_prefill_optimization.md) — prefill is FLOP-bound on M3 Ultra (quant doesn't help it); caching + sorted MoE + sparse-attn levers
- [Baked-artifact output path](project_artifact_output_path.md) — baked artifacts → `~/models/<source_name>-quanta_<type>`, all-resident, source never deleted
