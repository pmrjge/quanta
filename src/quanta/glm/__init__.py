"""GLM-5.1 (``glm_moe_dsa``) support for quanta — MLA + DSA Lightning-Indexer + noaux_tc MoE + 1 MTP head.

Submodules are imported directly (``quanta.glm.config``, ``quanta.glm.loader``, ``quanta.glm.tokenizer``,
…) to keep package import side-effect-free and avoid pulling MLX in before it is needed.
"""
